程序不会推出命令行模式。原因如下：
1.程序涉及类模板，需要将键盘上输入的数据转换成目标类型，工作量远超出预期
2.命令行模式相当于手动读取main函数的（测试类模板的）代码，解析代码的含义，再调用相关函数
3.在类模板的char模式下，如果要输入逗号或分号或减号，可能会和每条命令的分隔符造成冲突
4.当有重载的运算符时，情况会变得很难处理
5.如果有函数需要返回一个对象或者需要一个对象作为参数，事情会变得很麻烦

上述问题理论上都可以得到解决。但是这就几乎相当于把C++的编译器手动实现了一遍。实现命令行模式不会学到
任何新知识，反而造成很多麻烦，浪费很多时间